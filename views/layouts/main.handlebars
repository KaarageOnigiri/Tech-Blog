<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <nav>
            <a id="home-button" href="/">Home</a>
            <a id="dashboard=button" href="/dashboard">Dashboard</a>
            {{#if loggedIn}}
            <button id="logout">Logout</button>
            {{else}}
            {{!-- REMINDER to set route /login for login page --}}
            <a id="login" href="/login">Login</a>
            {{/if}}
        </nav>

        <div>
            <h1>
            {{!-- {{#if currentPage === 'home'}}
            The Tech Blog
            {{/if}}
            {{#if currentPage === 'dashboard'}}
            Your Dashboard
            {{/if}} --}}
            </h1>
        </div>
    </header>

    <main>
        {{{ body }}}
    </main>

    <footer>

    </footer>
    {{!-- REMINDER to set script.js file here --}}
    <script src="/js/script/js"></script>
</body>

</html>
